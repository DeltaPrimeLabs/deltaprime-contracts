# macOS Finder metadata (auto-created when browsing folders; never project-relevant)
.DS_Store

node_modules
build
.vscode

public
dist
cache
/artifacts/
# Foundry
/out
/cache_forge
.idea
.secrets
.secret
deployments/local
deployments/localhost
tools/liquidation/.private*

.vercel
.openzeppelin
typechain
tenderly.yaml

.env
domain-knowledge.md
work-goals.md
CLAUDE.local.md

# Claude Code per-machine local settings (permission allowlist; never shared). The
# **/ form also catches nested ones (e.g. tools/scripts/bridge/.claude/...).
.claude/settings.local.json
**/.claude/settings.local.json

# Generated artifacts (build output / script data, not source)
lambda/.serverless/*.zip
tools/scripts/snapshots-*.json
tools/scripts/unwind-gnosis-*.json

# Local git worktrees created by tooling/agents — never commit these (they get recorded as gitlinks)
.worktrees/
